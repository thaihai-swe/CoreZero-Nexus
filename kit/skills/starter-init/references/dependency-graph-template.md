# Dependency Graph

> Auto-generated by `/starter-init` (Phase A — brownfield archaeology). Part of the memory layer.
> Modules with `risk_flag: true` have high fan-in (>=5 dependents) — high blast radius.

- **Repository:** [PROJECT NAME]
- **Generated:** YYYY-MM-DD

## Risk Threshold

Modules with fan-in >= 5 are flagged `risk_flag: true`. Features touching these modules
must include blast-radius analysis in their `spec.md`.

## Module Map

<!-- One entry per significant module or subsystem. -->

```yaml
modules:
  - name: "[module-name]"
    path: "path/to/module"
    description: "What this module does."
    depends_on:
      - other-module-a
      - other-module-b
    depended_by:
      - consumer-module-x
      - consumer-module-y
    fan_in_count: 0
    risk_flag: false
    notes: ""
```

## High-Risk Modules (fan_in >= 5)

<!-- Auto-populated summary of risk_flag: true entries for quick scanning. -->

| Module | Fan-in | Path | Notes |
|---|---|---|---|
| _none identified_ | | | |

## Orphaned Modules

<!-- Modules with no dependents and no dependencies — candidates for deletion. -->

- _none identified_
