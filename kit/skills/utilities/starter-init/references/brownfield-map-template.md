# Brownfield Map

> Auto-generated by `/starter-init` (Phase A — brownfield archaeology). Part of the memory layer — loaded by sessions working on this repository.
> Do not edit manually unless re-running starter-init on this repository.

- **Repository:** [PROJECT NAME]
- **Generated:** YYYY-MM-DD
- **Sweep method:** Subagent read-only exploration

## Profile Rules

Any feature that touches a path rated `high` or `critical` in this map MUST be promoted
to `Complex` profile in its `status.md` before spec work begins. Check this map during
`/spec-requirements` profile selection.

## Severity Scale

| Severity | Meaning |
|---|---|
| `low` | Established, well-tested area. Standard rigor. |
| `medium` | Known debt or limited coverage. Standard rigor with extra care. |
| `high` | Fragile, poorly tested, or high blast radius. Promote to Complex. |
| `critical` | Active risk: no tests, production credentials, or cross-cutting contract. Promote to Complex. Require explicit user sign-off. |

## Areas

<!-- One entry per discrete area. Add as many as the sweep reveals. -->

### [AREA NAME]

```yaml
area: "[descriptive name]"
severity: low | medium | high | critical
description: "What this area does and why it's rated at this severity."
risky_paths:
  - path/to/file.ext
  - path/to/module/
implicit_contracts:
  - "Callers assume X behavior without it being documented anywhere."
recommended_action: "What should happen before a feature touches this area."
```

## Untested Surfaces

<!-- List of code surfaces with no test coverage identified during the sweep. -->

- _none identified_ or list paths

## Preserved Behaviors

<!-- Behaviors that MUST NOT change regardless of feature work. Minimum 3. -->

1. [BEHAVIOR ONE]
2. [BEHAVIOR TWO]
3. [BEHAVIOR THREE]

## Gaps in This Map

<!-- Areas the sweep could not evaluate (missing tooling, access denied, binary files, etc.). -->

- _none_
